#include "parse.hpp"
#include <iostream>



int parse(const string& json) {
    // for (char c : json) {
        // what can this character be?
        // note: ignore all spaces and newlines
    // }
    // another approach: track left *and* right (maybe an optimization problem later)

    return 0;
}
